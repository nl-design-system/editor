/** Heading level a document uses when it is not embedded under an existing outline. */
export const DEFAULT_TOP_HEADING_LEVEL = 1;

/** Deepest heading level HTML offers. */
export const MAX_HEADING_LEVEL = 6;
