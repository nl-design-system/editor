import { css } from 'lit';

export default css`
  :host {
    background-color: #fff;
    display: block;
    padding-block: var(--basis-space-inline-lg);
    padding-inline: var(--basis-space-inline-lg);
    position: relative;
  }
`;
