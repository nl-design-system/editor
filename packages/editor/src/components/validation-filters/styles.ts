import { css } from 'lit';

export default css`
  .clippy-validation-filters {
    display: flex;
    flex-wrap: wrap;
    gap: var(--basis-space-inline-md);
  }

  .clippy-validation-filters__filter {
    display: inline-flex;
  }
`;
