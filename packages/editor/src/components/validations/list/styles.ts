import { css } from 'lit';

export default css`
  .clippy-validations-list {
    display: grid;
    gap: var(--basis-space-inline-xl);
    list-style: none;
    margin-block: 0;
    padding-block: 0;
    padding-inline: 0;
  }
`;
